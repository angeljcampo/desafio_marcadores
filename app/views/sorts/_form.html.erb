<%= form_with(model: sort, local: true) do |form| %>
  <% if sort.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sort.errors.count, "error") %> prohibited this sort from being saved:</h2>

      <ul>
      <% sort.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :mark_id %>
    <%= form.text_field :mark_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
