<%= form_with(model: mark, local: false) do |form| %>
  <% if mark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mark.errors.count, "error") %> prohibited this mark from being saved:</h2>

      <ul>
      <% mark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

    <div class="field">
    <%= form.label :url %>
    <%= form.url_field :url %>
  </div>


  <div class="field">
    <%= form.label :subcategory_id %>
    <%= form.collection_select :subcategory_id, @subcategories, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit 'Create mark', data: { confirm: 'Are you sure?' } %>
  </div>
<% end %>
